#!/usr/bin/env node

console.log('Work!');

/* const commander = require('commander');

commander.version('0.0.4', '-v, --version')
.arguments('<firstConfig> <secondConfig>')
.description('Compares two configuration files and shows a difference.')
.option('-f, --format [type]', 'Output format').parse(process.argv);

if (commander.format) {
  console.log('Hello');
} */
